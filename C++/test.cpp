#include <map>
#include <set>
#include <list>
#include <cmath>
#include <ctime>
#include <deque>
#include <queue>
#include <stack>
#include <string>
#include <bitset>
#include <cstdio>
#include <limits>
#include <vector>
#include <climits>
#include <cstring>
#include <cstdlib>
#include <fstream>
#include <numeric>
#include <sstream>
#include <iostream>
#include <algorithm>
#include <unordered_map>

using namespace std;
int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int height, width;
    int s1, s2; // starting point row and column;
    int d1, d2; // destination point row and column;
    
    cin >> height >> width;
    cin >> s1 >> s2;
    cin >> d1 >> d2;
    
    int matrix[height][width];
    for(int i=0; i<height; i++)
    {
      string arrayString;
      cin >> arrayString;
      // get the 1s and 0s inside the matrix..

    }

    for(int i=0; i<height; i++)
    {
      for(int j=0; j<width; j++)
      {

      }
    }
    
    return 0;
}